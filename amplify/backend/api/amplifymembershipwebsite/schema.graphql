type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  body: String
  description: String
  readTime: Int
}

type Reviews @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  avatar: String
  review: String
}
